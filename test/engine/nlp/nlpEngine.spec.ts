describe('NLPEngine', () => {
  test.skip('TODO: should identify alethic assertoric candidates from a declarative sentence', () => {
    fail();
  });

  test.skip('TODO: should return empty candidates for non-declarative input (questions, commands)', () => {
    fail();
  });

  test.skip('TODO: should assign confidence scores to candidates', () => {
    fail();
  });
});
